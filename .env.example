# .env.example (frontend)
#
# Copia esto a ".env" en la raíz del proyecto frontend (no en backend/).
# Vite solo expone al navegador las variables que empiezan con VITE_.

VITE_API_URL=http://localhost:3000
